#ifndef VMM_H
#define VMM_H

#include <stdint.h>
#include <stddef.h>
#include "pmm.h"

// Refer to pmm.h for function definitions at the moment because we are trying to support
// __PMM_SAFE_ALLOCS_ALWAYS -- This is the crusty solution for now but hopefully it'll change soon

#endif // VMM_H